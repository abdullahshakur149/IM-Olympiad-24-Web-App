<main id="main" class="main">

    <div class="pagetitle">
        <h1>Dashboard</h1>
        <nav>
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="/admin-panel">Home</a></li>
                <li class="breadcrumb-item active">Dashboard</li>
            </ol>
        </nav>
    </div><!-- End Page Title -->

    <section class="section dashboard">
        <div class="row"></div>

        <div class="table-responsive">
            <table class="table table-bordered">
                <thead>
                    <tr class="bg-light">
                        <th scope="col" style="width: 4%;">#</th>
                        <th scope="col">Name </th>
                        <th scope="col">IMS Student</th>
                        <th scope="col">Contact Number</th>
                        <th scope="col">Action</th>

                    </tr>
                </thead>
                <tbody>
                    <% if (locals.registered_users) {%>
                        <% for (const [index,user] of registered_users.entries()) { %>
                            <tr>
                                <td><%=index+1%></td>
                                <td><%=user.name%></td>
                                <td><%=user.is_ims_student%></td>
                                <td><%=user.contact_number%></td>
                                <td><button type="button" class="trigger">Details</button></td>
                            </tr>
                        <% } %>
                    <% } %>
                </tbody>
            </table>
        </div>
    </section>
</main>